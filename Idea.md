# 一些想法

多智能体：
* 动态配置
* 自动派生
* 生命周期管理

文件相关Harness层：

* 设备路径黑名单：/dev/random、/dev/stdin 等，避免无限输出或阻塞
* 大文件提示：磁盘 >512KB、limit>200 且结果被截断时，附加 _hint 建议分段读
* 文件重复读可能会导致上下文冗余，考虑每个读取行为+命令取hash，记录读取次数，>=2则，不实际读取，而是返回一个占位符。注意当上下文压缩后，需要将清理重复次数，不然读不到原文了。